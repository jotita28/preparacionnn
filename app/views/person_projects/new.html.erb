<h3 class= "text-center">Asigna a un proyecto</h3>
<%= form_with model: PersonProject.new , url: person_person_projects_path(@person), local: true do |f|%>
<%= f.label :project%>
<%= f.select :project_id, @project_for_select,id: :project_id %>
<%= f.submit 'Assign'%>
<%end%>
